# Frontend Environment Variables for CapRover Deployment

# For CapRover deployment, the API will be served from the same domain
# So we use relative paths for API calls
VITE_API_BASE_URL=/api
VITE_NODE_ENV=production
